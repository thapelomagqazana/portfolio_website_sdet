/**
 * PostCSS configuration for Tailwind CSS v4.
 *
 * Keep this file small and explicit. Tailwind v4 uses the
 * `@tailwindcss/postcss` plugin instead of the older Tailwind v3 style setup.
 */
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
